import XCTest
@testable import PlayoffSimulator

final class PlayoffSimulatorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PlayoffSimulator().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
